#!/usr/bin/env bash
declare -A BROKEN_MAP=()
declare -g -A FINE_MAP=()
declare -ft some_function
my_func() {
declare -A local_map=()
}
if [[ -n "$X" ]]; then
    declare -A INDENTED_BUT_FILE_SCOPE=()
fi
